export function Pagination({ page, limit, total, onPageChange }) { const pages = Math.max(1, Math.ceil((total || 0) / (limit || 20))); if (pages <= 1) return null; return <div className="pagination"><button type="button" className="btn btn-secondary" disabled={page <= 1} onClick={() => onPageChange(page - 1)}>Previous</button><span className="muted">Page {page} of {pages} · {total || 0} items</span><button type="button" className="btn btn-secondary" disabled={page >= pages} onClick={() => onPageChange(page + 1)}>Next</button></div>; }
